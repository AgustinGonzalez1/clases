---
title: "Clase 8: Bloque teletransportador 🔀"
mision: "Tocar un bloque que se clone, se anime y luego desaparezca."
---

# Clase 8: Bloque teletransportador 🔀

Programamos un bloque teletransportador con un emisor y un receptor, y además un bloque que se clona, se anima y desaparece.

## Objetivos

- Teletransportar al jugador de un bloque a otro
- Usar Clone() y Destroy()
- Animar un objeto con TweenService

## Contenido

### 1. Crear el bloque emisor y el receptor

1. Insertás dos Parts en el Workspace
2. Una la nombrás `Emisor` (con el Script adentro)
3. La otra la nombrás `Receptor` (marca el destino, sin script)

```
Workspace
  ├── Emisor (Part con Script)
  └── Receptor (Part)
```

### 2. Programar el teletransporte

```lua
local emisor = script.Parent
local receptor = workspace.Receptor

emisor.Touched:Connect(function(mano)
    local personaje = mano.Parent
    personaje:MoveTo(receptor.Position)   -- mueve al jugador a la posicion del receptor
end)
```

> ⚠️ El bloque receptor tiene que llamarse exactamente `Receptor`. Si lo movés en el mapa, la teletransportación cambia sola sin tocar el código.

### 3. Crear un bloque que se clona, se anima y desaparece

```lua
local bloque = script.Parent
local TweenService = game:GetService("TweenService")
local activado = false

bloque.Touched:Connect(function()
    if activado then return end
    activado = true

    local clon = bloque:Clone()               -- crea una copia
    clon.Color = Color3.fromRGB(255, 0, 0)
    clon.Parent = workspace

    local info = TweenInfo.new(2)              -- duracion: 2 segundos
    local meta = {Position = clon.Position + Vector3.new(10, 5, 0)}
    local tween = TweenService:Create(clon, info, meta)
    tween:Play()

    wait(2)
    clon:Destroy()

    activado = false
end)
```

## Ejercicio

> Consigna: Programá un bloque que se clone, se anime y luego desaparezca al ser tocado.

**Pistas:**
1. Usá una variable `activado` para que no se creen varios clones a la vez
2. `TweenInfo.new(segundos)` define cuánto dura la animación
3. No olvides destruir el clon al final para no llenar el mapa

## Recursos

- [Documentación oficial de TweenService](https://create.roblox.com/docs/reference/engine/classes/TweenService)