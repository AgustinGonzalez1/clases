---
title: "Clase 14: Animaciones con Código 🎬"
mision: "Reproducir una animación al presionar una tecla."
---

# Clase 14: Animaciones con Código 🎬

Reproducimos animaciones del personaje mediante código al presionar una tecla. Como ejemplo extra, vemos cómo hacer una animación con el Tool equipado.

## Objetivos

- Cargar y reproducir una animación desde código
- Detectar teclas con UserInputService
- Combinar animación con el evento Activated de un Tool

## Contenido

### 1. El script principal (tecla F)

Va en **StarterPlayer → StarterCharacterScripts**.

```lua
local personaje = script.Parent
local humanoid = personaje:WaitForChild("Humanoid")
local animator = humanoid:WaitForChild("Animator")

local animacion = Instance.new("Animation")
animacion.AnimationId = "rbxassetid://TU_ID_ACA"
local track = animator:LoadAnimation(animacion)

local UIS = game:GetService("UserInputService")

UIS.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.F then
        track:Play()
    end
end)
```

### 2. Extra: Animación con el Tool

1. Copiás el Tool y lo pegás **dentro del rig** en el Workspace
2. Abrís el Animation Editor con el rig + Tool y animás el ataque
3. Publicás la animación

```lua
-- agregado al mismo script
personaje.ChildAdded:Connect(function(child)
    if child:IsA("Tool") then
        track.Priority = Enum.AnimationPriority.Action
        child.Activated:Connect(function()
            track:Play()
        end)
    end
end)
```

## Ejercicio

> Consigna: Programá una animación que se reproduzca al presionar la tecla F.

**Pistas:**
1. Recordá reemplazar `TU_ID_ACA` con el ID que publicaste en la Clase 4 o 5
2. `WaitForChild` evita errores por timing al cargar el Humanoid
3. Como extra, probá conectar la misma animación al click con un Tool equipado

## Recursos

- [Documentación oficial de UserInputService](https://create.roblox.com/docs/reference/engine/classes/UserInputService)